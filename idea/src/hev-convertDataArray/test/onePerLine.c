
#include <stdio.h>

main()
        {
        char token[1000];

        while (scanf ("%s", token) == 1)
                {
                printf ("%s\n", token);
                }

        return 0;
        }
