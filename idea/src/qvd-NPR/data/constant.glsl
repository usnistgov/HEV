// constants that control the look of the shader
const vec4 baseCol   = vec4(1.,1.,1.,1.); // basic rock
const vec4 textCol   = vec4(1.,0.,0.,1.); // text label color
const vec4 spotCol   = vec4(0.,0.,1.,1.); // spot color
const vec4 keyCol    = vec4(1.,0.,1.,1.); // high-stress at key frame
const vec4 stressCol = vec4(1.,1.,0.,1.); // high-stress at non-key frame

