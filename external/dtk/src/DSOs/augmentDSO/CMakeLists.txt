# CMakeLists.txt to build augmentDSOs

ADD_SUBDIRECTORY (dtk)

IF (UNIX)
	ADD_SUBDIRECTORY (dtkX11)

	IF (DTK_BUILD_WITH_GLX)
		ADD_SUBDIRECTORY (dtkGLX)
	ENDIF (DTK_BUILD_WITH_GLX)

	IF (DTK_BUILD_WITH_FLTK)
		ADD_SUBDIRECTORY (dtkFLTK)
	ENDIF (DTK_BUILD_WITH_FLTK)
ENDIF (UNIX)

