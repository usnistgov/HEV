# CMakeLists.txt to build sideWinderJoystick

INCLUDE_DIRECTORIES( ${DTK_SOURCE_DIR}/include )
ADD_LIBRARY( sideWinderJoystick MODULE sideWinderJoystick.cpp )
SET_TARGET_PROPERTIES( sideWinderJoystick PROPERTIES PREFIX "" )
INSTALL( TARGETS sideWinderJoystick LIBRARY DESTINATION etc/dtk/serviceDSO
		PERMISSIONS OWNER_READ OWNER_WRITE OWNER_EXECUTE GROUP_READ
		GROUP_EXECUTE WORLD_READ WORLD_EXECUTE )

