# CMakeLists.txt to build hello

INCLUDE_DIRECTORIES( ${DTK_SOURCE_DIR}/include )
ADD_LIBRARY( hello MODULE hello.cpp )
TARGET_LINK_LIBRARIES( hello dtk )
SET_TARGET_PROPERTIES( hello PROPERTIES PREFIX "" )
INSTALL( TARGETS hello LIBRARY DESTINATION etc/dtk/serviceDSO PERMISSIONS
		OWNER_READ OWNER_WRITE OWNER_EXECUTE GROUP_READ GROUP_EXECUTE
		WORLD_READ WORLD_EXECUTE )

