# CMakeLists.txt to build is900-2handed

INCLUDE_DIRECTORIES( ${DTK_SOURCE_DIR}/include )
ADD_LIBRARY( is900-2handed MODULE is900.cpp )
TARGET_LINK_LIBRARIES( is900-2handed dtk )
SET_TARGET_PROPERTIES( is900-2handed PROPERTIES PREFIX "" )
INSTALL( TARGETS is900-2handed LIBRARY DESTINATION etc/dtk/serviceDSO PERMISSIONS
		OWNER_READ OWNER_WRITE OWNER_EXECUTE GROUP_READ GROUP_EXECUTE
		WORLD_READ WORLD_EXECUTE )

