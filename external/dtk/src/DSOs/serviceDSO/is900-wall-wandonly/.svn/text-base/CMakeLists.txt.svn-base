# CMakeLists.txt to build is900

INCLUDE_DIRECTORIES( ${DTK_SOURCE_DIR}/include )
ADD_LIBRARY( is900-wall-wandonly MODULE is900.cpp )
TARGET_LINK_LIBRARIES( is900-wall-wandonly dtk )
SET_TARGET_PROPERTIES( is900-wall-wandonly PROPERTIES PREFIX "" )
INSTALL( TARGETS is900-wall-wandonly LIBRARY DESTINATION etc/dtk/serviceDSO PERMISSIONS
		OWNER_READ OWNER_WRITE OWNER_EXECUTE GROUP_READ GROUP_EXECUTE
		WORLD_READ WORLD_EXECUTE )

