# CMakeLists.txt to build programs

ADD_SUBDIRECTORY( sharedMemUtils )
ADD_SUBDIRECTORY( clientServer )
ADD_SUBDIRECTORY( generalUtils )
ADD_SUBDIRECTORY( augmentManager )

IF( DTK_BUILD_WITH_FLTK )
	ADD_SUBDIRECTORY( fltk )
ENDIF( DTK_BUILD_WITH_FLTK )

IF( NOT WIN32 AND XTEST_INCLUDE AND XTEST_LIBRARY )
	ADD_SUBDIRECTORY( xwand )
ENDIF( NOT WIN32 AND XTEST_INCLUDE AND XTEST_LIBRARY )

